import classes from "./RedirectToSSO.module.scss";

const RedirectToSSO: React.FC = () => {
  return (
    <span className={classes.text}>
      Redirecting you to SSO page, please wait
    </span>
  );
};

export default RedirectToSSO;
